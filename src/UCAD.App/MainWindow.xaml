<Window
    x:Class="UCAD.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UCAD">

    <Grid Background="#16191D">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton x:Uid="LineButton" Icon="Edit" Click="Line_Click"/>
            <AppBarButton x:Uid="ClearButton" Icon="Delete" Click="Clear_Click"/>
            <CommandBar.SecondaryCommands>
                <AppBarButton x:Uid="ResetViewButton" Click="ResetView_Click"/>
            </CommandBar.SecondaryCommands>
        </CommandBar>

        <local:CadViewport x:Name="Viewport"
                           Grid.Row="1"
                           BackgroundColor="#16191D"/>

        <Grid Grid.Row="2"
              Padding="12,6"
              Background="#20252B">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="ModeText" />
            <TextBlock x:Name="CoordinateText"
                       Grid.Column="1"
                       FontFamily="Consolas"
                       Text="X: 0.000   Y: 0.000"/>
        </Grid>
    </Grid>
</Window>
